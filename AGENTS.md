# Refining Sect Agent Instructions

本文件是《炼器宗》仓库的全局代理协作规则。它只保留高频、可执行的硬约束；游戏设计、模块边界、路线图和版本规则由对应权威文档承载，不在这里重复维护第二套口径。

## 1. 权威来源与冲突处理

在规划或修改仓库前，按任务范围阅读：

1. `README.md`：项目定位、核心玩法与当前技术方向；
2. `docs/ROADMAP.md`：大版本目标与玩家体验承诺；
3. `docs/MODULES.md`：一级模块、状态所有权、依赖方向与集成边界；
4. `docs/VERSIONING.md`：五段式页面版本号及其递进规则；
5. 受影响模块、组件、功能或工具的当前权威文档。

若用户决定、活动计划、仓库规则或模块文档彼此冲突，列出冲突来源，停止受影响的修改、提交和远端操作，并请用户裁决。不得静默选择更方便执行的一方。

没有被当前权威文档引用的历史资料、聊天记录和实验文件只能作为背景，不能覆盖当前规则。

## 2. 工作模式与授权边界

- 对话、解释和头脑风暴默认不修改仓库。
- 调查、诊断和审查默认只读；用户没有要求修复时不得顺手实施。
- 编写或修订计划只授权修改相应规划文档，不自动授权修改生产代码。
- 用户明确要求开发、修复或实施时，可以在请求范围内修改文件并完成相称验证。
- 未经用户明确要求，不得自动创建或切换分支、commit、tag、push、创建PR、将PR转为Ready、merge、release或删除远端分支。
- 用户对一次明确变更的授权不自动扩展到相邻功能、后续阶段或远端发布动作。

## 3. 文档变更与代码变更

纯文档变更可以按用户要求直接提交和推送到`main`，不要求创建PR，也不提升页面版本号。

以下内容属于代码变更，必须在具名开发分支中完成并通过PR交付，不得直接提交到`main`：

- 应用源码和测试；
- 构建、检查、测试、版本生成及其他可执行脚本；
- 依赖清单、锁文件和工具配置；
- 影响运行行为的内容数据、模式和配置；
- 与代码一同交付的配套文档。

同时包含文档和代码的变更按代码变更处理。Markdown中的示例代码不会仅因其存在而把纯说明文档变成代码变更；但能被工具消费、生成代码或改变运行契约的文档不属于纯说明文档。

分支命名规则尚未冻结。在正式规则建立前：

- 用户明确指定合法且无冲突的分支名时，按原文使用；
- 用户未指定时，不得擅自创建正式开发分支，应先与用户确认；
- 不得为了绕过这项待定规则而直接在`main`实施代码变更。

## 4. 实施基线与用户资产

首次修改前，检查并记录与任务相称的基线：

- 当前分支和`HEAD`；
- `git status --short`；
- 已修改、已暂存和未跟踪路径；
- 当前任务范围、非目标、验证方式和预期交付路径。

预先存在的修改和未跟踪文件属于用户。不得覆盖、删除、暂存、提交或混入当前工作；若它们与任务冲突，先报告冲突。

禁止使用`git reset --hard`、破坏性checkout或其他可能抹除用户工作的操作。已发布变更需要回滚时，优先使用可审查、可恢复的方式，例如`git revert`。

## 5. Ponytail：最小充分实现

《炼器宗》默认采用Ponytail full模式。理解需求并追踪受影响流程后，按顺序选择第一个能够完整满足请求的方案：

1. 不做：行为已经存在，或当前阶段不需要；
2. 复用仓库现有的类型、组件、工具或模式；
3. 使用语言标准能力；
4. 使用浏览器、HTML、CSS和Svelte原生能力；
5. 使用已经安装的依赖；
6. 一行清晰逻辑足以表达时直接实现；
7. 最后才编写最小而完整的新代码。

执行规则：

- 先搜索仓库并追踪调用方、状态流和所有权，再选择修改位置。
- 在共同边界修复一次根因，不在多处粘贴症状补丁。
- 优先删除而非增加，优先朴素代码而非聪明抽象，优先少改文件。
- 不为单一实现创建抽象层，不为假想需求添加配置、扩展点、空目录或脚手架。
- 不引入现有能力能够替代的新依赖。
- 不为了让架构显得完整而扩大任务范围。
- 用户明确要求的完整功能必须实现，不得以“简化”为由擅自缩减。

Ponytail不得剪除输入校验、安全与隐私控制、防止数据丢失或状态损坏的错误处理、基础无障碍能力、必要测试、游戏核心不变量或用户明确要求。

若当前实现有意接受一个已知上限，使用项目语言留下可测量的升级条件：

```text
ponytail: <当前上限>；当<可测量条件>出现时升级
```

## 6. 范围与轻量治理

- 严格遵守当前任务的范围、非目标和验收条件。
- 不得以“顺手整理”“顺便重构”或“最佳实践”为由扩大范围。
- 新发现的问题记录为风险、阻塞或后续工作；除非它阻止当前请求，否则不自动纳入。
- `docs/MODULES.md`中的15个一级模块是长期责任边界，不代表必须立即建立15个代码目录。
- 不为v2.0至v5.0提前创建空壳实现、兼容层、后端服务或桌面封装。

除非用户以后明确采用相应制度，否则不得从Novelix或其他项目机械复制以下流程：

- 每日工作日志和跨日交接；
- `New_Created_Files.md`清单；
- Phase、Stage、Round或Wave体系；
- NEST测试资产登记；
- 每阶段rollback tag；
- TaskPacket、Permit或连续自动化窗口。

## 7. 模块边界与状态所有权

- 每一份权威状态只能由`docs/MODULES.md`定义的一个模块拥有和写入。
- 跨模块协作使用明确命令、领域事件和只读快照，不得直接修改其他模块内部状态。
- UI、存档和运行时不是领域状态的第二权威来源。
- 运行时只负责装配唯一模块实例，不复制领域规则或状态。
- 集成故障应回到规则所属模块修复，不在胶水层重写一套补丁逻辑。
- 对外使用稳定标识和明确契约，不依赖相邻模块的内部对象结构。
- 修改一级模块职责、依赖方向或状态所有权时，必须同步更新`docs/MODULES.md`，并按`docs/VERSIONING.md`判断版本层级。

## 8. 游戏模拟硬约束

- 游戏模拟必须与页面渲染和动画表现分离；动画帧率不得决定资源产量、耗时或成功判定。
- 在线推进与离线结算必须调用同一套领域规则，在相同输入下产生等价结果。
- 逻辑时钟、随机性和时间结算应可复现、可测试。
- 五行纯转化遵守质能守恒，转化效率不得超过100%；任何天然来源、奖励或外部注入都必须有明确来源。
- 损耗、灵气逸散、容量、堵塞和停机必须存在于真实模拟中，不能只作为视觉效果。
- 界面显示的阵纹、流向、库存和生产状态必须来源于真实模拟快照。
- 核心流程不得因耗尽不可再生的启动资源而进入无法恢复的软锁；若设计允许失败，必须存在明确恢复路径。
- 数值表示、舍入和大数规则必须集中定义，不得由各界面或模块自行发明。

具体配方、数值、灵石兑换和剧情条件以当前游戏设计权威文档及后续正式数据为准，不在本文件复制。

## 9. 存档与版本

- 存档从首个可玩版本开始携带明确的结构版本。
- 改变持久化状态时，必须同时考虑读取、校验、迁移、失败恢复和旧存档兼容。
- 不得静默丢弃无法识别的玩家进度，也不得把“可重新计算”作为删除数据的默认理由。
- 页面版本号只从未来确定的单一权威文件读取，不得在多个组件复制版本字符串。
- 每个代码PR都必须按`docs/VERSIONING.md`判断并更新页面版本；纯文档变更不提升页面版本。
- 第五段总代码行数只能由未来的统一脚本生成，不得手工猜测。

## 10. 依赖与技术选择

- 当前技术方向以`README.md`为准；技术选型尚未进入依赖清单前，不视为已经安装。
- 新增依赖前，先证明标准能力、浏览器原生能力、Svelte原生能力和现有依赖均不足以完成任务。
- 引入依赖时记录用途、替代方案、许可、包体积、维护状态和移除路径。
- 不提前引入额外状态管理框架、Canvas/WebGL引擎、数据库、后端、账户系统、云存档、插件系统或Electron。
- 达到路线图阶段或出现可测量瓶颈后，再单独评估上述能力。

## 11. 验证与证据

- 验证结果必须来自实际执行；没有运行的命令不得声称通过。
- 先运行与变更直接相关的聚焦检查，再按风险扩大到全量检查。
- 非平凡行为必须留下一个在回归时会失败的可运行检查；优先使用仓库已有测试风格和最小责任边界。
- 琐碎声明式或单行变更不要求仪式性测试，但仍需检查实际差异。
- 至少对文本变更执行`git diff --check`。
- 工程脚手架建立后，格式化、类型检查、单元测试、浏览器测试和构建命令以`README.md`及包脚本为准，不得从历史计划猜测命令。
- 区分自动测试通过、人工体验验收、平台验证和发布准备；不得用一个“通过”替代全部证据。

## 12. GitHub、远端与Workflow

- GitHub PR和仓库操作优先使用已认证的本地`gh`。
- 本地分支、commit和tag使用原生Git；fetch、pull和push优先使用SSH remote。
- 不得静默修改remote URL、认证方式、凭证或仓库权限。
- 不得在命令、日志、remote URL或文档中暴露token、API Key和其他秘密。

本仓库禁用GitHub Actions和GitHub Workflow：

- 不得创建、恢复、修改、启用或执行`.github/workflows/**`；
- 不得把Actions结果作为实施、验证、发布或验收证据；
- 不得用远端Workflow代替缺失的本地测试或平台环境；
- 若发现已存在的Workflow，将其视为仓库规则漂移并先报告，不得自行触发或处理。

## 13. 文档与语言

- 《炼器宗》自行撰写的规划、设计、架构、治理、验收和风险文档默认使用简体中文。
- 代码标识符、API、包名、原始错误、命令输出、专有名词和准确引用可以保留原文。
- 文档跟随其所有者：全仓规则和跨模块设计进入`docs/`，模块专属资料放在对应模块边界内。
- 不在根目录创建`PLAN/`、`REPORT/`、`AUDIT/`、`OUTPUT/`或其他临时过程目录。
- 根级`README.md`提供项目入口，不承担全部详细制度。
- 文档内的相对链接、命令和路径在提交前必须实际核对。

## 14. 完成定义

不得仅因代码已经写完就声称任务完成。交付至少需要：

- 用户要求的范围和验收条件已经满足；
- 适用验证已经实际执行并准确报告；
- 页面版本、存档迁移和关联文档已按变更需要更新；
- 风险、主动留白和未验证平台边界已经说明；
- 没有混入、覆盖或删除用户原有修改；
- 代码变更存在可审查的开发分支和PR；
- 没有伪造命令结果、URL、远端状态或验收结论。

## 15. 本文件维护

根级`AGENTS.md`只保留全仓高频硬规则和权威路由。只有子目录出现真正不同的命令、边界或覆盖需求时，才在最近目录增加下级`AGENTS.md`，不得为了分类美观复制根规则。

当同类错误重复发生，或新流程已经实际成为稳定制度时，再评估更新本文件或增加机械门禁。不要为想象中的问题把本文件扩张成另一份施工手册。
